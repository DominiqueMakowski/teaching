{
  "name": "agentic-programming-slides",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "slidev slides.md --open",
    "build": "slidev build slides.md --out dist --base ./ && rm -rf assets && cp -r dist/index.html . && cp -r dist/assets . && rm -rf dist",
    "pdf": "slidev export slides.md --output AgenticProgramming.pdf"
  },
  "dependencies": {
    "@slidev/cli": "^52.20.1",
    "@slidev/theme-default": "^0.25.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@slidev/types": "^52.20.1"
  }
}
